"use client";

import { StaticPageTemplate } from "@/components/layout/StaticPageTemplate";

/**
 * Content comes from the `page:shipping-delivery` template in the theme customizer.
 */
export default function Page() {
  return <StaticPageTemplate slug="shipping-delivery" title="Shipping & Delivery" />;
}
